<tr>
  <td class="line-height6"><nobr>
    <span class="badge">
      <i class="icon-eye-<%= repo.private ? 'close' : 'open' %> icon-white"></i>
      <%= repo.watchers_count %>
    </span>
  </nobr></td>
  <td>
    <h3>
      <%= link_to repo.owner, user_path(repo.owner) %> /
      <%= link_to "https://github.com/#{repo.owner}/#{repo.original_name}", class: 'external-link', target: '_blank' do %>
        <%= repo.original_name %> <i class="icon-share-alt"></i>
      <%- end -%>
    </h3>
    <p class="muted"><%= repo.description %></p>
  </td>
  <td class="muted"><h6 class="line-height6"><%= repo.language %></h6></td>
  <td class="line-height6"><nobr>
    <span class="badge">
      <i class="icon-random icon-white"></i>
      <%= repo.forks %>
    </span>
  </nobr></td>
</tr>